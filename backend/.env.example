# Copy this file to .env and fill values before local run (not needed on Cloud Run)
GCP_PROJECT=YOUR_PROJECT_ID
VERTEX_LOCATION=us-central1
# AI endpoints names might be like:
TEXT_MODEL="text-bison@001"        # optional, adjust if calling specific endpoint
EMBEDDING_MODEL="textembedding-gecko@001"  # optional
